require 'spec_helper'

describe Faraday::RequestResponseLogger do
  it 'has a version number' do
    expect(Faraday::RequestResponseLogger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
